@model PL.Models.PageViewModels.NationalityListPageViewModel

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Nacionalnosti</h2>
<hr />

@if (Model.Success)
{
    @Html.RouteLink("Kreiraj novi", new { action = "Create", controller = "Nationality" }, new { @class = "btn btn-default col-md-12" })
    <table class="table table-bordered table-striped col-md-12">
        <thead>
            <tr>
                <th class="col-md-10">Nacionalnost</th>
                <th class="col-md-2">Opcije</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var nationality in Model.NationalityViewModels)
                {
                <tr>
                    <td>@nationality.Nationality</td>
                    <td>
                        <a href="@Url.Action("Edit", "Nationality", new { id = nationality.NationalityId })" class="btn btn-primary-table btn-xs"><span class="glyphicon glyphicon-pencil"></span></a>
                        <a href="@Url.Action("Delete", "Nationality", new { id = nationality.NationalityId })" class="btn btn-primary-table btn-xs" onclick="return confirm('Da li ste sigurni da zelite da obrisete odabranu nacionalnost?')"><span class="glyphicon glyphicon-trash"></span></a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <div class="row">
        <div class="col-md-12">
            <div class="alert-danger">
                <a href="#" class="close" data-dissmiss="alert" aria-label="close">&times;</a>
                <strong>Greska!</strong> @Model.Message
            </div>
        </div>
    </div>
}



